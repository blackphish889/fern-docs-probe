---
title: Import and Module Injection
subtitle: Testing esbuild module resolution
slug: import-injection
---

import ferncfg from '../fern.config.json'

## Import Path Traversal Tests

### I1: fern.config.json via Relative Import

<pre>{JSON.stringify(ferncfg, null, 2)}</pre>

### I2: Function Constructor

{new Function('return document.domain')()}

### I3: Type Probes

{typeof process !== 'undefined' ? 'process-exists' : 'no-process'}

{typeof global !== 'undefined' ? 'global-exists' : 'no-global'}

{typeof require !== 'undefined' ? 'require-exists' : 'no-require'}
